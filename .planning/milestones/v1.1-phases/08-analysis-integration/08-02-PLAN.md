---
phase: 08-analysis-integration
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [app.py]
autonomous: false
requirements: [ANLYS-03]

must_haves:
  truths:
    - "User can select any two companies and view them side-by-side"
    - "Comparison view shows KPI scorecard for both companies"
    - "Comparison view shows key charts for both companies"
    - "Comparison mode is clearly distinct from single-company mode"
  artifacts:
    - path: "app.py"
      provides: "Comparison view toggle, dual-company selection, side-by-side dashboard rendering"
      contains: "compare"
  key_links:
    - from: "app.py (sidebar)"
      to: "comparison toggle and second dropdown"
      via: "st.checkbox or st.toggle for compare mode"
      pattern: "compare"
    - from: "app.py (comparison view)"
      to: "calculate_kpis + build_scorecard"
      via: "runs pipeline for both companies"
      pattern: "calculate_kpis.*compare"
---

<objective>
Add a side-by-side comparison view where users select two companies and see their KPIs, scorecards, and key charts rendered in parallel columns.

Purpose: The comparison view is the capstone feature of v1.1 -- it lets hiring managers see how two companies stack up, which is the core value of a financial health dashboard.
Output: Updated app.py with comparison mode toggle, dual company selection, and side-by-side rendering.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analysis-integration/08-01-SUMMARY.md

<interfaces>
<!-- From 08-01, the executor will have: -->
<!-- st.session_state.uploaded_companies: dict[str, pd.DataFrame] -->
<!-- companies list: get_all_companies() + list(st.session_state.uploaded_companies.keys()) -->

From src/kpi_engine.py:
```python
def calculate_kpis(df: pd.DataFrame) -> pd.DataFrame:
def get_latest_kpis(df: pd.DataFrame) -> dict:
```

From src/benchmarks.py:
```python
def build_scorecard(kpis: dict) -> list[dict]:
RAG_COLORS: dict  # {"Green": "#00c896", "Amber": "#f5a623", "Red": "#e84545"}
BENCHMARKS: dict[str, KPIBenchmark]
```

From src/insights.py:
```python
def generate_insights(scorecard: list[dict], df: pd.DataFrame, kpis: dict) -> list[dict]:
```

Key design tokens already defined in app.py:
```python
NAVY, CARD_BG, BORDER, TEXT_WHITE, TEXT_MUTED, ACCENT, TEAL, AMBER, RED
CHART_LAYOUT  # dict applied to all Plotly figures
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build comparison view with dual-company selection</name>
  <files>app.py</files>
  <action>
Add a comparison mode to app.py. This is an additive change -- single-company mode stays exactly as-is, comparison mode adds a new rendering path.

1. **Sidebar: Add comparison toggle** (after the company dropdown):
   - Add `compare_mode = st.checkbox("Compare two companies")` below the existing selectbox
   - When checked, show a second selectbox: `compare_company = st.selectbox("Compare with", [c for c in companies if c != selected_company])`
   - Style the checkbox label consistently with existing sidebar typography

2. **Data pipeline for comparison** (after the existing data pipeline block):
   - When `compare_mode` is True:
     - Load data for both companies using the same logic as single mode (session_state check for uploaded, load_data for sample)
     - Compute `df_full_b`, `df_b`, `kpis_b`, `scorecard_b`, `insights_b` for the second company
     - Rename existing variables to `_a` suffix for clarity: `df_a = df`, `kpis_a = kpis`, etc.

3. **Comparison rendering** (replace the 5 dashboard sections when compare_mode is True):
   When compare_mode is True, render a DIFFERENT layout instead of the standard 5 sections:

   **Header**: Show both company names with their overall RAG status side by side.

   **Section 1 - KPI Comparison Table**:
   - One row per KPI (6 rows)
   - Columns: KPI Name | Company A Value | Company A RAG | Company B Value | Company B RAG
   - Use existing RAG colors for status badges
   - Highlight which company is "better" on each KPI with a subtle indicator

   **Section 2 - Dual Radar Chart**:
   - Single radar chart with BOTH companies overlaid (different colors)
   - Company A: ACCENT blue, Company B: TEAL green
   - Reuse the existing normalize_kpi() function
   - Keep the benchmark dashed line

   **Section 3 - Revenue Trend Comparison**:
   - Single chart with both companies' revenue lines overlaid
   - Company A solid line (ACCENT), Company B dashed line (TEAL)
   - Shared x-axis (month_label)

   **Section 4 - Key Metrics Cards**:
   - Two columns, each showing the 5 metric cards for their respective company
   - Reuse the existing metric_card() function

4. **When compare_mode is False**: render the existing 5-section dashboard exactly as before. No changes to single-company mode.

5. **Styling**: Use the existing design tokens (CARD_BG, BORDER, TEXT_WHITE, TEXT_MUTED, ACCENT, TEAL). The comparison view should feel like a natural extension of the dashboard, not a separate page.

Key implementation note: Use `if compare_mode:` / `else:` to branch between comparison and single-company rendering. Do NOT try to merge the two paths -- keep them cleanly separated. The single-company path must remain untouched.
  </action>
  <verify>
    <automated>python -c "
source = open('app.py').read()
assert 'compare' in source.lower(), 'Missing compare mode'
assert source.count('selectbox') >= 2, 'Need at least 2 selectbox calls'
print('Comparison structure checks passed')
"</automated>
  </verify>
  <done>
  - Sidebar shows "Compare two companies" checkbox
  - When checked, a second company dropdown appears
  - Comparison view renders KPI table, dual radar, revenue overlay, and metric cards for both companies
  - Single-company mode is completely unchanged
  - Both sample and uploaded companies can be compared
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of comparison view</name>
  <files>app.py</files>
  <action>Human verifies the comparison view in the running dashboard.</action>
  <what-built>Side-by-side company comparison view</what-built>
  <how-to-verify>
    1. Run `streamlit run app.py`
    2. Verify single-company mode works identically to before (NovaSaaS, CloudForge)
    3. Check "Compare two companies" checkbox
    4. Verify second dropdown appears showing the other companies
    5. Select two different sample companies -- verify comparison view renders:
       - KPI comparison table with RAG colors for both
       - Dual radar chart with both companies overlaid
       - Revenue trend with both lines
       - Side-by-side metric cards
    6. Upload a CSV file, then compare uploaded company vs a sample company
    7. Uncheck comparison mode -- verify single-company view returns
  </how-to-verify>
  <verify>Human confirms all 7 verification steps pass</verify>
  <done>Comparison view renders correctly for any two companies, single-company mode unaffected</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `streamlit run app.py` starts without errors
- Comparison checkbox toggles between single and comparison mode
- Comparison view renders all 4 comparison sections
- Both sample and uploaded companies can be compared
- Single-company mode unchanged
</verification>

<success_criteria>
- ANLYS-03: User can select any two companies and view their KPIs, charts, and scorecard side-by-side in a comparison view
</success_criteria>

<output>
After completion, create `.planning/phases/08-analysis-integration/08-02-SUMMARY.md`
</output>
