---
phase: 06-file-input
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - app.py
autonomous: false
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
must_haves:
  truths:
    - "User can upload a CSV or Excel file via the sidebar without leaving the dashboard"
    - "User can download a pre-formatted template file from the sidebar"
    - "Dashboard loads NovaSaaS and CloudForge sample data by default when no file is uploaded"
    - "When a file is uploaded, the dashboard renders using that file's data instead of sample data"
  artifacts:
    - path: "app.py"
      provides: "Sidebar upload widget, template download button, data source switching"
      contains: "st.file_uploader"
  key_links:
    - from: "app.py"
      to: "src/template.py"
      via: "import read_uploaded_file, get_template_csv_bytes"
      pattern: "from src\\.template import"
    - from: "app.py"
      to: "src/data_generator.py"
      via: "fallback to generate_company_data when no upload"
      pattern: "generate_company_data"
    - from: "app.py sidebar upload"
      to: "app.py data pipeline"
      via: "session_state or conditional branch"
      pattern: "file_uploader|uploaded_file"
---

<objective>
Wire the upload widget, template download button, and data source switching into the dashboard sidebar.

Purpose: This is the user-facing integration. After this plan, users can upload their own CSV/Excel file and see the dashboard analyze it, or download a template to understand the expected format. When no file is uploaded, the existing sample companies load as before (zero-friction default).

Output: Modified `app.py` with upload/download UI and data source branching logic
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-file-input/06-01-SUMMARY.md

@app.py
@src/template.py
@src/data_generator.py
@src/kpi_engine.py
</context>

<interfaces>
<!-- From src/template.py (created in Plan 01): -->
<!--
  REQUIRED_COLUMNS: list[str] = ["date", "revenue", "cogs", "opex", "headcount", "cash_balance"]
  generate_template_df() -> pd.DataFrame  # 3 example rows
  read_uploaded_file(uploaded_file) -> pd.DataFrame  # parses CSV/Excel, adds derived columns
  get_template_csv_bytes() -> bytes  # for st.download_button
-->

<!-- From src/data_generator.py (existing): -->
<!--
  generate_company_data(company_name: str) -> pd.DataFrame
  get_all_companies() -> list[str]
  get_company_profile(company_name: str) -> dict
-->

<!-- From src/kpi_engine.py (existing): -->
<!--
  calculate_kpis(df: pd.DataFrame) -> pd.DataFrame
  get_latest_kpis(df: pd.DataFrame) -> dict
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add upload widget, download button, and data source switching to app.py</name>
  <files>app.py</files>
  <action>
Modify app.py to add file upload and data source switching. All changes stay within the existing code structure.

1. **Add import** at the top of app.py alongside existing src imports:
   `from src.template import read_uploaded_file, get_template_csv_bytes`

2. **Add upload section to sidebar** — insert AFTER the company selectbox/profile card block (after line ~213, before the footer text), but BEFORE the date range slider. The upload section should include:

   a. A styled section header matching existing sidebar aesthetics:
      ```
      st.markdown(f'<div style="font-size:11px; font-weight:700; color:{TEXT_MUTED}; letter-spacing:1px; text-transform:uppercase; margin-bottom:10px; margin-top:24px;">Upload Your Data</div>', unsafe_allow_html=True)
      ```

   b. `st.file_uploader("Upload CSV or Excel", type=["csv", "xlsx", "xls"], key="file_upload", help="Upload a file matching the template format")` — store result in `uploaded_file`

   c. `st.download_button(label="Download Template CSV", data=get_template_csv_bytes(), file_name="smb_financial_template.csv", mime="text/csv")` — styled below the uploader

   d. A small help text: "Upload your company's monthly financials. Download the template to see the required format."

3. **Modify data pipeline section** (lines ~225-235) to branch on upload state:

   ```python
   # ── Data Pipeline ──
   if uploaded_file is not None:
       try:
           df_uploaded = read_uploaded_file(uploaded_file)
           df_full = calculate_kpis(df_uploaded)
           # Override company display info
           selected_company = uploaded_file.name.rsplit(".", 1)[0]  # filename without extension
           is_uploaded = True
       except Exception as e:
           st.sidebar.error(f"Could not read file: {e}")
           # Fall back to sample data
           df_full = load_data(selected_company, v="v2")
           is_uploaded = False
   else:
       df_full = load_data(selected_company, v="v2")
       is_uploaded = False
   ```

   Keep the rest of the pipeline (`df = df_full.tail(month_range)...`) unchanged.

4. **Conditionally show/hide company profile card**: When `is_uploaded` is True, the company profile card in the sidebar should show the uploaded filename instead of the hardcoded profile. Wrap the existing profile card in `if not is_uploaded:` and add an else branch showing a simple "Uploaded: {filename}" card with the same styling.

5. **Conditionally show/hide company selectbox**: The selectbox for sample companies should still appear, but add a note when a file is uploaded. Use `st.sidebar.info("Clear the uploaded file to switch back to sample companies.")` when `is_uploaded` is True.

   IMPORTANT: Because Streamlit executes top-to-bottom, the sidebar widgets (selectbox, uploader) must be defined before the data pipeline. Structure the sidebar as:
   - Company selectbox (existing)
   - Company profile card (existing, conditional on not is_uploaded)
   - Upload section (new: uploader + download button)
   - Date range slider (existing)
   - Footer (existing)

   Then after the sidebar `with` block, the data pipeline uses `uploaded_file` to decide the source.

6. **Update Dashboard Guide text** to mention upload capability. Change the "What this is" paragraph to include: "Upload your own CSV/Excel data via the sidebar, or explore two fictional SaaS companies."

Do NOT modify any chart sections, KPI calculations, or other dashboard sections. The data pipeline output (df, kpis, scorecard, insights) has the same shape regardless of source.
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Desktop/SMB Financial Health Dashboard" && python -c "
import ast, sys
with open('app.py') as f:
    source = f.read()
# Check import
assert 'from src.template import' in source, 'Missing template import'
assert 'read_uploaded_file' in source, 'Missing read_uploaded_file'
assert 'get_template_csv_bytes' in source, 'Missing get_template_csv_bytes'
# Check upload widget
assert 'file_uploader' in source, 'Missing file_uploader widget'
assert 'download_button' in source, 'Missing download_button widget'
# Check data branching
assert 'uploaded_file' in source, 'Missing uploaded_file variable'
assert 'is_uploaded' in source, 'Missing is_uploaded flag'
# Check it parses as valid Python
try:
    ast.parse(source)
    print('Valid Python syntax')
except SyntaxError as e:
    print(f'SYNTAX ERROR: {e}')
    sys.exit(1)
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>
    - Sidebar shows file upload widget accepting CSV/Excel
    - Sidebar shows template download button
    - Sample companies load by default when no file is uploaded
    - Uploaded file data flows through the existing KPI/chart pipeline
    - app.py is valid Python with no syntax errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify upload flow end-to-end</name>
  <what-built>File upload widget, template download, and data source switching in the sidebar. Sample companies still load by default.</what-built>
  <action>Human verification checkpoint — run streamlit app and test upload flow manually.</action>
  <files>app.py</files>
  <verify>User confirms upload, download, and default mode all work correctly.</verify>
  <done>User approves the upload flow end-to-end.</done>
  <how-to-verify>
    1. Run `streamlit run app.py` and verify the dashboard loads with NovaSaaS by default (INPUT-03)
    2. In the sidebar, confirm you see "Upload Your Data" section with a file uploader and "Download Template CSV" button
    3. Click "Download Template CSV" — verify it downloads a CSV with headers: date, revenue, cogs, opex, headcount, cash_balance and 3 example rows (INPUT-02)
    4. Upload the downloaded template CSV — verify the dashboard switches to showing that data (3 months of example data). Charts and KPIs should render without errors (INPUT-01)
    5. Remove the uploaded file (click X) — verify it falls back to sample company data
    6. Switch between NovaSaaS and CloudForge — both should still work as before
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Dashboard loads with sample data by default (no upload)
- Upload widget accepts .csv, .xlsx, .xls files
- Template download produces well-formatted CSV
- Uploading the template CSV renders the dashboard with that data
- Clearing upload reverts to sample data
- No regressions in existing company views
</verification>

<success_criteria>
- INPUT-01: User can upload CSV/Excel via sidebar — verified by uploading the template
- INPUT-02: User can download a pre-formatted template — verified by clicking download button
- INPUT-03: Sample companies load by default — verified by initial page load showing NovaSaaS
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-input/06-02-SUMMARY.md`
</output>
